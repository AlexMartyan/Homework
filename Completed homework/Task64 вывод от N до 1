// Задайте значение N. Напишите программу, которая выведет
// все натуральные числа в промежутке от N до 1.

int N = 5;
Console.WriteLine(Output(N));

string Output (int num)
{
    if (num == 1)
        return num.ToString();
    return num.ToString() + Output(num-1);
}
